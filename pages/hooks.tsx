import { Fragment } from 'react';
import { HookState } from '../components/part';

// any타입 & 함수 state 설정하기
const Hooks = (props: any) => {
    return (
        <Fragment>
            <HookState />
        </Fragment>
    );
};

export default Hooks;
